/**
 * Native useStripe hook — re-exports from @stripe/stripe-react-native.
 * On web, Metro will load use-stripe.web.ts instead (no native imports).
 */
export { useStripe, initStripe } from "@stripe/stripe-react-native";
